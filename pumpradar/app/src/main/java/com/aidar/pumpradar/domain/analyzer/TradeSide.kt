package com.aidar.pumpradar.domain.analyzer

/**
 * Сторона гипотетической сделки для двустороннего анализа (LONG + SHORT).
 *
 * Важно: PumpRadar — сканер сигналов, а НЕ торговый бот. Сторона используется
 * только для теневой (paper) оценки исхода по историческим/будущим данным цены.
 * Реальные ордера не отправляются, API-ключ не требуется.
 */
enum class TradeSide { LONG, SHORT }
